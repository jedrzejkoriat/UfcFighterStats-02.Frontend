type FightModel = {
    result: string | null,
    opponent: string | null,
    eventName: string | null,
    date: string | null,
    method: string | null,
    round: number | null,
    time: string | null
};

export default FightModel;